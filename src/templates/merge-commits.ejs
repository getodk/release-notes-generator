<% commits.forEach(function (commit) { %>- <% if (commit.title.startsWith("Merge pull request")) { %><%= commit.messageLines[0] %> (#<%= commit.pr %>)<% } else { %><%= commit.title %><% } %>
  - <%= commit.author.buildSignature() %><% }) %>
